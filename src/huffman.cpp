#include <iostream>
#include <vector>
#include <huffman.h>
using namespace std;

void compressHuffman(const string& inputFile,const string& outputFile)
{
    cout << "[Huffman] Ccompressing " << inputFile << " -> " << outputFile << " (Coming soon!)\n";
}

void decompressHuffman(const string& inputFile,const string& outputFile)
{
    cout << "[Huffman] Decompressing " << inputFile << " -> " << outputFile << " (Coming soon!)\n";
}