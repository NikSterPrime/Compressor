#include <iostream>
#include <vector>
#include <huffman.h>
#include <bits/stdc++.h>
using namespace std;

class Node {
	public:
		int data;
		Node *left,*right;
		Node(int x)
		{
			data = x;
			left = nullptr;
			right = nullptr;
		}
};

void compressHuffman(const string& inputFile,const string& outputFile)
{
		
}

